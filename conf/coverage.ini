[run]
source = rexlib

[paths]
# The tests import the installed package, so the report would name
# site-packages, and Sonar can only match what the repository has.
source =
    python/rexlib
    */site-packages/rexlib

[xml]
output = coverage.xml
