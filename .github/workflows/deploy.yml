name: Deploy release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
    branches:
      - main

  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/deploy.yml'
      - 'pyproject.toml'

  workflow_dispatch:

jobs:
  generate_stubs:
    name: Generate type stubs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.13'

      - name: Set up compiler cache
        uses: hendrikmuhs/ccache-action@d62db5f07c26379fc4b4e0916f098a92573c3b03 # v1.2.23
        with:
          key: stubs-ubuntu-latest
          max-size: 1G

      - name: Install rexlib
        run: |
          python -m pip install "cmake>=3.18" ninja
          cmake -S external/rexlib -B "${{ runner.temp }}/rexlib-build" \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ runner.temp }}/rexlib" \
            -DCMAKE_INSTALL_LIBDIR=lib \
            -DBUILD_TESTING=OFF \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build "${{ runner.temp }}/rexlib-build" \
            --config Release --parallel 4
          # Everything except the namelink, which a wheel cannot hold.
          cmake --install "${{ runner.temp }}/rexlib-build" \
            --config Release --component rexlib_runtime
          cmake --install "${{ runner.temp }}/rexlib-build" \
            --config Release --component rexlib_development

      - name: Install with pip
        env:
          CMAKE_PREFIX_PATH: ${{ runner.temp }}/rexlib
        run: |
          pip install . -v \
            -Ccmake.define.REXLIB_PYTHON_STAGE_PREFIX="${{ runner.temp }}/rexlib"

      - name: Collect the generated stubs
        run: |
          mkdir -p stubs
          cp "$(python -c 'import rexlib, pathlib; print(pathlib.Path(rexlib.__file__).parent)')"/_binding/*.pyi stubs/
          ls -l stubs

      - name: Upload stubs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: type-stubs
          path: stubs/*.pyi

  build_wheels:
    needs: generate_stubs
    name: Build wheels for ${{ matrix.os }} (${{ matrix.architectures }})
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            architectures: "x86_64"
          - os: ubuntu-24.04-arm
            architectures: "aarch64"
          - os: macos-latest
            architectures: "x86_64"
          - os: macos-latest
            architectures: "arm64"
          - os: macos-latest
            architectures: "universal2"
          - os: windows-latest
            architectures: "AMD64"
          - os: windows-11-arm
            architectures: "ARM64"

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true

      - name: Download the type stubs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: type-stubs
          path: stubs

      # macOS builds run on the runner itself, so the usual setup applies.
      # Windows is left out: ccache with MSVC needs the debug information
      # format pinned, and the build degrades to an uncached one anyway.
      - name: Set up compiler cache (macOS)
        if: runner.os == 'macOS'
        uses: hendrikmuhs/ccache-action@d62db5f07c26379fc4b4e0916f098a92573c3b03 # v1.2.23
        with:
          key: wheels-${{ matrix.os }}-${{ matrix.architectures }}
          max-size: 2G

      - name: Build wheels
        uses: pypa/cibuildwheel@1828c10ab37f080699c7b81cea34097c684a7074 # v4.2.0
        env:
          CIBW_ARCHS: "${{ matrix.architectures }}"
          # rexlib is built and installed once per platform, before any
          # wheel, so each interpreter only compiles the binding. Building
          # it per wheel took ~22 minutes each and ran the x86_64 job into
          # GitHub's six hour ceiling.
          CIBW_BEFORE_ALL: >
            python -m pip install "cmake>=3.18" ninja &&
            cmake -S {project}/external/rexlib -B /tmp/rexlib-build
            -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/tmp/rexlib
            -DCMAKE_INSTALL_LIBDIR=lib -DBUILD_TESTING=OFF &&
            cmake --build /tmp/rexlib-build --config Release
            --parallel 4 &&
            cmake --install /tmp/rexlib-build --config Release
            --component rexlib_runtime &&
            cmake --install /tmp/rexlib-build --config Release
            --component rexlib_development
          CIBW_BEFORE_ALL_WINDOWS: >
            python -m pip install "cmake>=3.18" ninja &&
            cmake -S {project}/external/rexlib -B C:/rexlib-build
            -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=C:/rexlib
            -DCMAKE_INSTALL_LIBDIR=lib -DBUILD_TESTING=OFF &&
            cmake --build C:/rexlib-build --config Release
            --parallel 4 &&
            cmake --install C:/rexlib-build --config Release
            --component rexlib_runtime &&
            cmake --install C:/rexlib-build --config Release
            --component rexlib_development
          # A platform-specific value replaces the general one rather than
          # adding to it, so the shared part is repeated.
          CIBW_ENVIRONMENT: >-
            SKBUILD_CMAKE_DEFINE="REXLIB_PYTHON_STUBS_DIR=stubs;REXLIB_PYTHON_STAGE_PREFIX=/tmp/rexlib"
            CMAKE_PREFIX_PATH=/tmp/rexlib
          CIBW_ENVIRONMENT_WINDOWS: >-
            SKBUILD_CMAKE_DEFINE="REXLIB_PYTHON_STUBS_DIR=stubs;REXLIB_PYTHON_STAGE_PREFIX=C:/rexlib"
            CMAKE_PREFIX_PATH=C:/rexlib
          CIBW_SKIP: "cp3??t-win*" # Skip Python free-threaded builds on Windows
          # librexlib now ships inside the wheel, put there by this
          # project's install rules. auditwheel would otherwise re-vendor
          # it under a hash-mangled SONAME, which breaks find_package for
          # plugin authors and the sibling plugin directory lookup.
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            auditwheel repair -w {dest_dir} {wheel}
            --exclude "librexlib.so*"
          CIBW_REPAIR_WHEEL_COMMAND_MACOS: >
            delocate-wheel --require-archs {delocate_archs}
            -w {dest_dir} -v {wheel}
            --ignore-missing-dependencies
          # rexlib.dll is placed beside the extension by this project's
          # own install rules; letting delvewheel vendor it would mangle
          # the name the plugin directory is resolved from.
          CIBW_REPAIR_WHEEL_COMMAND_WINDOWS: >
            delvewheel repair -w {dest_dir} -v {wheel}
            --exclude rexlib.dll

      - name: Upload wheels
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true

      - name: Build sdist
        run: pipx run build --sdist

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: cibw-sdist
          path: dist/*.tar.gz

  upload_pypi:
    name: Upload to PyPI
    needs: [build_wheels, build_sdist]
    if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: cibw-*
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -l ./dist

      - name: Upload to PyPI # TODO enable this in the future
        if: false
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1

  create_developer_release:
    name: Create development release
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event_name == 'push' && github.event.ref == 'refs/heads/main'
    env:
      TAG_NAME: "development"
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Download artifacs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "cibw-*"
          path: dist
          merge-multiple: true

      - name: Create or update development tag
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'action@github.com'
          git add dist
          git commit -m "Add pre-built binaries"
          git tag -fa $TAG_NAME -m 'Update development tag with main branch'
          git push origin $TAG_NAME --force

      - name: Delete all old assets from development release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view $TAG_NAME --json assets -q ".assets[].name" | while read asset; do
            gh release delete-asset $TAG_NAME "$asset" -y
          done

      - name: Create or update development release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: "Development build"
          body: >-
            This is an automated release containing binaries built with 
            with the head of the main branch.
          draft: false
          prerelease: true
          files: dist/**
